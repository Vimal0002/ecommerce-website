# This file shows the environment variables needed for production deployment
# Copy these to your Netlify dashboard under Site Settings > Environment Variables

# Database - Use a cloud database for production (PostgreSQL, PlanetScale, etc.)
DATABASE_URL="file:./dev.db"

# JWT Secret - Generate a strong secret for production
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"

# NextAuth Secret - Generate a strong secret for production
NEXTAUTH_SECRET="your-nextauth-secret-change-this-in-production"

# Your Netlify site URL (update after deployment)
NEXTAUTH_URL="https://your-site-name.netlify.app"

# App Settings
NODE_ENV="production"